version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'weekly'
      day: 'saturday'
    versioning-strategy: 'increase'
    labels:
      - 'dependencies'
    open-pull-requests-limit: 5
    pull-request-branch-name:
      separator: '-'
    commit-message:
      # cause a release for non-dev-deps
      prefix: fix(deps)
      # no release for dev-deps
      prefix-development: chore(dev-deps)
    ignore:
      - dependency-name: '@salesforce/dev-scripts'
      - dependency-name: 'ink'
      # handled by the dedicated daily block below
      - dependency-name: '@salesforce/agents'
      - dependency-name: '*'
        update-types: ['version-update:semver-major']

  # Check for @salesforce/agents updates daily (the most frequent Dependabot supports)
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'daily'
    versioning-strategy: 'increase'
    labels:
      - 'dependencies'
    open-pull-requests-limit: 5
    pull-request-branch-name:
      separator: '-'
    commit-message:
      prefix: fix(deps)
    allow:
      - dependency-name: '@salesforce/agents'
    ignore:
      - dependency-name: '@salesforce/agents'
        update-types: ['version-update:semver-major']
